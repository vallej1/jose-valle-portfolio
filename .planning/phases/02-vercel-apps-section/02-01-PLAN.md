---
phase: 02-vercel-apps-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/portfolio.ts
  - components/VercelApps.tsx
  - app/page.tsx
  - components/Header.tsx
  - next.config.ts
autonomous: false

must_haves:
  truths:
    - "Visitor sees an 'Apps in Production' section between Case Studies and the Chat section"
    - "Section shows exactly 4 app cards: Quiz App, Expense Splitter, Chain-of-Thought Demo, Punch List"
    - "Each card displays: color accent bar, app name, description, tech tags, and a 'View App' link"
    - "Clicking any card opens the live Vercel app in a new browser tab"
    - "Header nav contains an 'Apps' link that scrolls to the section"
    - "Cards animate in on scroll (fade up) and show hover states (border + text color change)"
  artifacts:
    - path: "data/portfolio.ts"
      provides: "VercelApp interface and vercelApps array with 4 entries"
      contains: "export interface VercelApp"
    - path: "components/VercelApps.tsx"
      provides: "Animated client component rendering app cards in a 4-column grid"
      exports: ["default function VercelApps"]
    - path: "app/page.tsx"
      provides: "Page composition with VercelApps between CaseStudies and ChatBox"
      contains: "<VercelApps />"
    - path: "components/Header.tsx"
      provides: "Nav item linking to #apps anchor"
      contains: "href: \"#apps\""
    - path: "next.config.ts"
      provides: "Remote image domain allowlist for **.vercel.app"
      contains: "**.vercel.app"
  key_links:
    - from: "components/VercelApps.tsx"
      to: "data/portfolio.ts"
      via: "named import"
      pattern: "import.*vercelApps.*from.*data/portfolio"
    - from: "app/page.tsx"
      to: "components/VercelApps.tsx"
      via: "default import and JSX render"
      pattern: "<VercelApps"
    - from: "components/Header.tsx"
      to: "section#apps"
      via: "anchor href"
      pattern: "href.*#apps"
---

<objective>
Verify the "Apps in Production" section is correctly implemented across all 5 modified files and renders properly in the browser.

Purpose: The implementation is already complete. This plan confirms all artifacts are correctly wired, TypeScript compiles cleanly, and the section displays as intended — matching the dark-theme design system used throughout the portfolio.
Output: Confirmed working "Apps in Production" section visible on the portfolio homepage.
</objective>

<execution_context>
@C:/Users/jeval/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jeval/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@data/portfolio.ts
@components/VercelApps.tsx
@app/page.tsx
@components/Header.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify implementation integrity across all 5 files</name>
  <files>
    data/portfolio.ts
    components/VercelApps.tsx
    app/page.tsx
    components/Header.tsx
    next.config.ts
  </files>
  <action>
    Run the following checks to confirm each artifact is correctly implemented:

    1. TypeScript compilation — confirm zero errors:
       npx tsc --noEmit

    2. Verify VercelApp interface exists in data/portfolio.ts with required fields (id, name, description, url, tags, color) and all 4 apps are present (quiz-app, expense-splitter, chain-of-thought, punch-list).

    3. Verify VercelApps.tsx:
       - Has "use client" directive
       - Imports useInView from framer-motion
       - Renders section with id="apps"
       - Maps over vercelApps to render animated cards
       - Each card is an anchor tag with target="_blank" rel="noopener noreferrer"
       - Color accent bar uses app.color
       - Tech tags rendered as pill spans
       - ExternalLink icon rendered in the "View App" link footer

    4. Verify app/page.tsx:
       - Imports VercelApps from "@/components/VercelApps"
       - Renders <VercelApps /> between <CaseStudies /> and <ChatBox />

    5. Verify Header.tsx navItems array contains { label: "Apps", href: "#apps" }.

    6. Verify next.config.ts remotePatterns includes hostname "**.vercel.app".

    If any check fails, fix the specific issue in the relevant file. Do not modify files that pass their checks.
  </action>
  <verify>npx tsc --noEmit exits with code 0 and produces no output</verify>
  <done>All 5 files pass structural checks and TypeScript reports zero type errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Apps section in browser</name>
  <files>
    components/VercelApps.tsx
  </files>
  <action>
    Start the dev server with `npm run dev` and open http://localhost:3000.
    The Apps section is already rendered — no code changes needed. This task confirms the live browser experience matches the design intent before the phase is closed.
  </action>
  <verify>
    With the dev server running at http://localhost:3000:
    - Scroll to the Apps section — 4 cards visible with correct app names and color bars
    - Click any card — opens the corresponding Vercel app in a new tab
    - Click "Apps" in the header nav — page scrolls to the section
    - Hover a card — border and title change to the primary accent color
  </verify>
  <done>
    All 4 app cards display correctly with name, description, tags, and live link.
    Nav "Apps" link scrolls to the section. Hover states work. User types "approved".
  </done>
  <what-built>
    "Apps in Production" section with 4 animated app cards, nav link, and live Vercel URLs:
    - Quiz App (blue accent) — https://quiz-project-pied-phi.vercel.app/
    - Expense Splitter (green accent) — https://expense-splitter-mocha.vercel.app/app
    - Chain-of-Thought Demo (purple accent) — https://scout-chain-of-thought-demo.vercel.app/
    - Punch List (amber accent) — https://punch-list-xi.vercel.app/
  </what-built>
  <how-to-verify>
    1. Start the dev server:
       npm run dev

    2. Open http://localhost:3000 in your browser.

    3. Scroll down past the Case Studies section — you should see "Apps in Production" as the next section.

    4. Confirm the section shows:
       - Section heading "Apps in Production"
       - Subtitle "Side projects and demos shipped to Vercel..."
       - 4 cards in a grid (4 columns on large screens, 2 on tablet, 1 on mobile)
       - Each card has a colored top bar, app name, description, tech tags, and "View App" link

    5. Hover over a card — confirm the border changes to the primary accent color and the app name changes color.

    6. Click "Quiz App" — confirm it opens https://quiz-project-pied-phi.vercel.app/ in a new tab.

    7. Click "Chain-of-Thought Demo" — confirm it opens https://scout-chain-of-thought-demo.vercel.app/ in a new tab.

    8. In the header nav, confirm "Apps" appears as a nav item. Click it — confirm the page scrolls to the Apps section.

    9. Resize to mobile width (< 640px) — confirm cards stack to a single column.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Overall phase checks:

1. `npx tsc --noEmit` — must exit 0 with no output
2. `npm run build` — must complete without errors (confirms next.config.ts remotePatterns are valid)
3. All 4 app URLs resolve when opened in a browser (live Vercel deployments)
4. Header "Apps" nav link scrolls to section#apps anchor
5. Section position: after CaseStudies, before ChatBox
</verification>

<success_criteria>
Phase 02 is complete when:
- "Apps in Production" section is visible on the portfolio homepage
- All 4 app cards display correct name, description, tags, and color accent
- Each card links to the correct live Vercel URL in a new tab
- Header nav "Apps" link scrolls to the section
- Hover animations work on cards (border + text color transition)
- Scroll-triggered fade-up animation triggers when section enters viewport
- TypeScript and Next.js build pass with zero errors
- Section visually matches the dark-theme design system (same surface/border/text CSS variables as other sections)
</success_criteria>

<output>
After completion, create `.planning/phases/02-vercel-apps-section/02-01-SUMMARY.md` documenting:
- What was verified (all 5 files)
- TypeScript/build status
- Visual verification result
- Any issues found and fixed
</output>
